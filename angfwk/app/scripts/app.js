'use strict';

var CashApp = {},
    app;

CashApp.User = {};

// Declare app level module which depends on views, and components
app = angular.module('cashClientApp', ['ngRoute', 'LocalStorageModule']);



